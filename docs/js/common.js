// 別ファイルで定義したVueのコンポーネントを読み込む
// * 以下のスクリプトが使える状態にしておく必要がある
// * ・Vue
// * ・axios
// function getComponents(src) {
//     axios.get(src).then(response => {
//         eval(response.data);
//     });
// }